services:
  - type: web
    name: vegan-matcher-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash ./startup.sh
    # Don't store API keys here - add them in the Render dashboard
    # envVars:
    #   - key: YELP_API_KEY
    #     sync: false # Use the Render dashboard to set this value securely
    #   - key: OPENCAGE_API_KEY
    #     sync: false # Use the Render dashboard to set this value securely
